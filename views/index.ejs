<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Index</title>
    </head>
    <body>
        Index page
        <table>
            <tr>
                <th>ID</th>
                <th>Username</th>
                <th>Password</th>
            </tr>
            <% users.forEach(user => { %>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.username %></td>
                <td><%= user.password %></td>
            </tr>
            <% }) %>
        </table>
        <a href="http://localhost:3300/users">Users</a>
        <a href="http://localhost:3300/login">Login</a>
        <form action="/logout" method="post">
            <button type="submit">Logout</button>
        </form>
        <a href="http://localhost:3300/register">Register</a>
    </body>
</html>
